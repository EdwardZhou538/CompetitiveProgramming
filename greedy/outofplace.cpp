#include <bits/stdc++.h>

using namespace std;
int main() {

    ifstream fin;
    ofstream fout;

    fin.open("outofplace.in");
    int n;
    fin >> n;

    int order[n];
    for (int i = 0; i < n; i++) fin >> order[i];

    for (int i = 0; i < n-1; i++) {
        if (order[i] > order[i+1]) {
            
        }
    }
}