#include <bits/stdc++.h>

using namespace std;
int main() {
    freopen("photoshoot.in", "r", stdin);

    int n;
    cin >> n;

    char order[n];
    string s;
    cin >> s;

    for (int i = 0; i < n; i++) {
        order[i] = s.at(i);
    }

    
}